const q=(s,r=document)=>r.querySelector(s);const qa=(s,r=document)=>Array.from(r.querySelectorAll(s));qa("img").forEach(i=>i.addEventListener("error",()=>i.classList.add("img-miss")));const nav=q(".nav-toggle"),menu=q(".main-nav");if(nav&&menu)nav.addEventListener("click",()=>menu.classList.toggle("is-open"));const slides=qa(".hero-slide"),dots=qa(".hero-dot");let heroIndex=0;function showHero(n){if(!slides.length)return;heroIndex=(n+slides.length)%slides.length;slides.forEach((s,i)=>s.classList.toggle("is-active",i===heroIndex));dots.forEach((d,i)=>d.classList.toggle("is-active",i===heroIndex))}dots.forEach((d,i)=>d.addEventListener("click",()=>showHero(i)));if(slides.length>1)setInterval(()=>showHero(heroIndex+1),5000);qa(".quick-search").forEach(f=>f.addEventListener("submit",e=>{e.preventDefault();const v=f.querySelector("input").value.trim();location.href="./search.html"+(v?"?q="+encodeURIComponent(v):"")}));const searchList=q(".search-list");if(searchList){const params=new URLSearchParams(location.search),kw=q("#kw"),year=q("#year"),region=q("#region"),type=q("#type"),empty=q(".empty-result");if(params.get("q"))kw.value=params.get("q");function run(){const k=(kw.value||"").trim().toLowerCase(),y=year.value,r=region.value,t=type.value;let shown=0;qa(".movie-card",searchList).forEach(c=>{const text=(c.dataset.title+" "+c.dataset.genre+" "+c.dataset.region+" "+c.dataset.type).toLowerCase();const ok=(!k||text.includes(k))&&(!y||c.dataset.year===y)&&(!r||c.dataset.region.includes(r))&&(!t||c.dataset.type.includes(t));c.style.display=ok?"":"none";if(ok)shown++});if(empty)empty.style.display=shown?"none":"block"}qa("input,select",q(".filter-box")).forEach(el=>el.addEventListener("input",run));run()}