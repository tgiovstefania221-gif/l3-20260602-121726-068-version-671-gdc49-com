import{H as Hls}from"./hls-dru42stk.js";function bind(btn){const video=document.getElementById(btn.dataset.player),src=btn.dataset.src;if(!video||!src)return;let ready=false;async function start(){btn.classList.add("is-hidden");if(!ready){if(video.canPlayType("application/vnd.apple.mpegurl")){video.src=src}else if(Hls&&Hls.isSupported()){const hls=new Hls({enableWorker:true,lowLatencyMode:true});hls.loadSource(src);hls.attachMedia(video);video._hls=hls}else{video.src=src}ready=true}video.controls=true;try{await video.play()}catch(e){}}btn.addEventListener("click",start);video.addEventListener("click",()=>{if(video.paused)start()})}document.querySelectorAll(".play-mask[data-src]").forEach(bind);